import CustomerContainer from "@/components/screens/customers";
import React from "react";

type Props = {};

const LoginPage = (props: Props) => {
  return <CustomerContainer />;
};

export default LoginPage;
